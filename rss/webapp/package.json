{
  "name": "@weco/rss",
  "version": "1.0.0",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "build": "tsc && yarn bundle",
    "bundle": "cp package.json dist && cd dist && yarn install --production && zip -r stories_rss.zip . && cd ..",
    "uploadToS3": "yarn build && node deploy.cjs"
  },
  "devDependencies": {
    "aws-sdk": "^2.836.0"
  },
  "dependencies": {
    "prismic-client-beta": "npm:@prismicio/client@beta",
    "rss": "^1.2.2"
  },
  "type": "module"
}
