version: '3.3'
services:
  eventbrite_webapp:
    image: wellcome/wellcomecollection_catalogue_webapp:${CONTAINER_TAG:-test}
    build:
      context: ../
      dockerfile: ./build/Dockerfile_nextapp
      args:
        - WEBAPP_NAME=catalogue
    ports:
      - "3000:3000"
