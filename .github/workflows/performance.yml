name: Performance

on:
  pull_request:
    paths-ignore:
      - '.buildkite/**'
      - 'assets/**'
      - 'cache/**'
      - 'cardigan/**'
      - 'catalogue/terraform/**'
      - 'content/terraform/**'
      - 'docs/**'
      - 'identity/terraform/**'
      - 'infrastructure/**'
      - 'playwright/**'
      - 'prismic-model/**'
      - 'preview/**'

jobs:
  bundle_size_diff:
    name: Bundle size diff
    runs-on: ubuntu-20.04
    steps:
      - run: "Hello world!"