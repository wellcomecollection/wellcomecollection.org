import title from './parts/title';
import body from './parts/body';
import promo from './parts/promo';

const Page = {
  Page: {
    title,
    body
  },
  Promo: {
    promo
  }
};

export default Page;
