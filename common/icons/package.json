{
  "name": "@weco/icons",
  "description": "Icon set and optimisation for wellcomecollection.org",
  "license": "MIT",
  "scripts": {
    "compile": "svgr svgs --out-dir components --no-svgo --typescript --template template.js && yarn makeIndex",
    "makeIndex": "node make-index.js"
  },
  "devDependencies": {
    "@svgr/cli": "^5.5.0"
  }
}
