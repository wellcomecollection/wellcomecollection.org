import { Meta, Story, ArgsTable } from '@storybook/addon-docs';
import Pagination from '@weco/common/views/components/Pagination/Pagination';
import Readme from '@weco/common/views/components/Pagination/README.md';
import * as stories from './Pagination.stories.tsx';

<Meta title={`Components/Pagination`} component={Pagination} />

<Readme />

If somebody is midway through a list of pages, we show both the previous and next buttons:
  <Story story={stories.middleOfPagination} />


If somebody is at the start or end of a list of pages, we only show the next/previous button as appropriate, for example:
  <Story story={stories.startOfPagination} />

<ArgsTable />
