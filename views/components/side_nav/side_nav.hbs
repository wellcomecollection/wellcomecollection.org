<nav class="side-nav">
  <ul class="side-nav__list">
    {{#each items as |item|}}
      <li key={{item.title}} class="side-nav__item">
        <a class="side-nav__link" href={{item.url}}>{{item.title}}</a>
      </li>
    {{/each}}
  </ul>
</nav>
