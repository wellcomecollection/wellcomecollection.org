<div class="tweet" {% if model.id %}data-track-click='{"screenName": "{{ model.screenName }}", "createdAt": "{{ model.createdAt }}", "postId": {{ model.id }}}'{% endif %}>
  {{ model.html | safe}}
</div>
