<div class="header">
  <div class="header__upper">
    <div class="header__inner container">
      <button id="header-burger-trigger"
        class="header__burger-trigger"
        aria-controls="header-nav"
        aria-label="menu">
        <span></span>
        <span></span>
        <span></span>
      </button>
      <a href="#" class="header__brand">
        {# TODO: use svg-sprite logo #}
        <img class="header__logo" src="https://wellcomecollection.org/sites/all/themes/col_base/images/wellcomecollection-logo.svg" />
      </a>
    </div>
  </div>
  <div class="header__lower container">
    <nav id="header-nav"
      class="header__nav"
      aria-labelledby="header-burger-trigger">
      <ul class="header__list">
        {% for navLink in navLinks %}
          <li class="header__item {{ "is-current" if navLink.isCurrent }}">
            <a href="{{ navLink.url }}" class="header__link">{{ navLink.title }}</a>
          </li>
        {% endfor %}
      </ul>
    </nav>
  </div>
</div>
