<div class="author {% for modifier in modifiers %} author--{{ modifier }} {% endfor %}">
  <div class="author__upper">
    {% if image %}
      <div class="author__image-wrap">
        <img class="author__image" src="{{ image | convertImageUri(64) }}" alt="Image of {{ name }}" />
      </div>
    {% endif %}
    <div class="author__details">
      <span class="author__name {{ {s:'WB6'} | fontClasses }}">{{ name }}</span>
      {% if twitterHandle %}
        <span class="author__twitter {{  {s:'HNM6'} | fontClasses }}">
          <a href="https://twitter.com/{{ twitterHandle }}"
            class="author__link"
            aria-label="{{ name }} (screen name: {{ twitterHandle }})">
            {% icon 'social/twitter' %} @{{ twitterHandle }}
          </a>
        </span>
      {% endif %}
    </div>
  </div>
  {% if description %}
    <div class="author__description {{ {s:'LR3', m:'LR2'} | fontClasses }}">{{ description | safe }}</div>
  {% endif %}
</div>
