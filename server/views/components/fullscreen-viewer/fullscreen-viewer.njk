{% set viewerId = '' | generateId %}
{# TODO - use button component? #}
<div class="js-fullscreen-viewer fullscreen-viewer">
  <button role="button" class="{{ {s:'HNM5'} | fontClasses }} {{ {s: 2} | spacingClasses({margin: ['top']}) }} btn btn--dark fullscreen-viewer__launch-button js-enter-fullscreen" data-target="{{viewerId}}">
    {% icon 'actions/expand', null, extraIconClasses %}
    <span class="button-text">view larger image</span>
  </button>
  <div class="fullscreen-viewer-content" id="{{viewerId}}" data-info-src="{{model.imageUrl | convertImageUri(full, false) | convertIiifUriToInfoUri}}">
    <div class="fullscreen-viewer__controls flex flex-end flex--v-center {{ {s: 1} | spacingClasses({margin: ['bottom']}) }}">
      <button class="{{ {s:'HNM5'} | fontClasses }} {{ {s: 1} | spacingClasses({margin: ['left']}) }} btn btn--dark" id="zoom-in-{{viewerId}}">
        {% icon 'actions/zoom-in' %}
        <span class="button-text">zoom in</span>
      </button>
      <button class="{{ {s:'HNM5'} | fontClasses }} {{ {s: 1} | spacingClasses({margin: ['left']}) }} btn btn--dark" id="zoom-out-{{viewerId}}">
        {% icon 'actions/zoom-out' %}
        <span class="button-text">zoom out</span>
      </button>
      {# <button class="{{ {s:'HNM5'} | fontClasses }} {{ {s: 1} | spacingClasses({margin: ['left']}) }} btn btn--dark" id="zoom-reset-{{viewerId}}">
        {% icon 'actions/refresh' %}
        <span class="button-text">zoom reset</span>
      </button> #}
      {# <button class="{{ {s:'HNM5'} | fontClasses }} {{ {s: 1} | spacingClasses({margin: ['left']}) }} btn btn--dark" id="rotate-l-{{viewerId}}">rotate left</button>
      <button class="{{ {s:'HNM5'} | fontClasses }} {{ {s: 1} | spacingClasses({margin: ['left']}) }} btn btn--dark" id="rotate-r-{{viewerId}}">rotate right</button> #}
      <button role="button" title="exit fullscreen" class="{{ {s:'HNM5'} | fontClasses }} {{ {s: 1} | spacingClasses({margin: ['left']}) }} btn btn--dark js-exit-fullscreen" id="zoom-reset-{{viewerId}}">
        {% icon 'actions/clear' %}
        <span class="button-text">exit fullscreen</span>
      </button>
    </div>
    <div class="fullscreen-viewer__image" id="image-viewer-{{viewerId}}"></div>
  </div>
</div>
{# TODO rename classes? #}
