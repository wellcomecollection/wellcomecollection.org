<div class="image-gallery">
  {% for item in imageGallery.items %}
    <div class="image-gallery__item"> {{item.type}}
      {% component 'image', { image: item, maxOutHeight: true, galleryImage: true } %}
    </div>
  {% endfor %}
</div>
