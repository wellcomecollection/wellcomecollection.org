<div class="pagination float-r flex-inline flex--center text--meta margin-top-2 margin-bottom-2">
  {% if (model.prevPage) %}
    <a href="?page={{ model.prevPage }}" rel="prev" class="margin-r-2">
      <div class="icon-rounder flex flex--center">
        {% icon 'actions/arrow-big', 'Previous (page '+ model.prevPage +')', ['icon--90', 'icon--2x', 'h-center'] %}
      </div>
    </a>
  {% endif %}

  <span>Page {{ model.currentPage }} of {{ model.pageCount }}</span>

  {% if (model.nextPage) %}
    <a href="?page={{ model.nextPage }}" rel="next" class="margin-left-2">
      <div class="icon-rounder flex flex--center">
        {% icon 'actions/arrow-big', 'Next (page '+ model.nextPage +')', ['icon--270', 'icon--2x', 'h-center'] %}
      </div>
    </a>
  {% endif %}
</div>
