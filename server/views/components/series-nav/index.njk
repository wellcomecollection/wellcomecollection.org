<div class="series-nav">
  {% for item in list.items.toJS() %}
    <div class="series__item{{ " series__item--selected" if current == item.url  }}">
      {{ item.title }}
    </div>
  {% endfor %}
</div>
