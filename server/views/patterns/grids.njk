{% extends "views/layout/patterns.njk" %}
{% block body %}

<h1>Layout</h1>

<!--
// - Minimum width: 240px??
// - Maximum width: 1424px
-->
<h2>Breakpoints</h2>

<p>The layout utilises 3 breakpoints:</p>

<ul>
    <li>Small: >= 0</li>
    <li>Medium: >= 600px</li>
    <li>Large: >= 960px</li>
</ul>

<h2>Spacing</h2>

<p>Breakpoints determine the number of columns in the layout, the spacing between the columns, 
 i.e. gutters, and the space either side of the layout, i.e. margins.</p>

<h3>Small</h3>

<ul>
    <li>Number of columns: 4</li>
    <li>Width of gutters: 18px </li>
    <li>Width of margins: 30px</li>
</ul>

<h3>Medium</h3>

<ul>
    <li>Number of columns: 8</li>
    <li>Width of gutters: 24px</li>
    <li>Width of margins: 42px</li>
</ul>

<h3>Large</h3>

<ul>
    <li>Number of columns: 12</li>
    <li>Width of gutters: 30px</li>
    <li>Width of margins: 60px</li>
</ul>

<h2>Markup</h2>

<h3>Basic grid</h3>

<p>Layout grids are created using to main classes, '.grid' and '.grid__cell'.</p>

<pre><code>&lt;div class="grid"&gt;
    &lt;div class="grid__cell"&gt;
        {content of the cell goes here}
    &lt;/div&gt;
&lt;/div&gt;</code></pre>

<p>Any number of grid cells can be placed in grid container and 
by default they will be equally spaced out.</p>

<div class="grid">
    {% include "views/components/grid-placeholder.njk" %}
    {% include "views/components/grid-placeholder.njk" %}
</div>

<div class="grid">
    {% include "views/components/grid-placeholder.njk" %}
    {% include "views/components/grid-placeholder.njk" %}
    {% include "views/components/grid-placeholder.njk" %}
</div>

<h3>Size modifiers</h3>

<p>Modifier classes are available, related to each of the available breakpoints,
which determine how many columns a cell will take up.</p>

<pre><code>&lt;div class="grid"&gt;
    &lt;div class="grid__cell grid__cell--s2 grid__cell--m2 grid__cell--l4"&gt;
        {content of the cell goes here}
    &lt;/div&gt;
&lt;/div&gt;</code></pre>

<!-- need to be able to pass in class to component -->

<p>In this example, the . Remember, the number of columns available, changes at each of the breakpoints,
so while at the large breakpoint l1-l12 are available, at the small breakpoint only s1-s4 are available.</p>

<h3 class="styleguide__item-subheading">Grid 1 - breakout</h3>
<div class="grid grid--breakout">
    {% include "views/components/grid-placeholder.njk" %}
</div>

{% endblock %}
