{% extends 'layout/default.njk' %}

{% block body %}
<div class="row {{ {s:5} | spacingClasses({padding: ['top']}) }}">
  <div class="container">
    <div class="grid">
      <div class="{{ {s:12, m:11, l:8, xl:7} | gridClasses }}">
        <div class="body-content">
          {% markdown %}
            {% include '../docs/PROGRESS_NOTES.md' %}
          {% endmarkdown %}
        </div>
      </div>
    </div>
  </div>
</div>
{% endblock %}
