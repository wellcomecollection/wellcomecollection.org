{% extends 'layout/default.njk' %}

{% block body %}
  {% set newsletterModel = {
    isSuccess: isSuccess,
    isError: isError,
    isConfirmed: isConfirmed
  } %}

  {% if featuresCohort == 'wellcomeUser' %}
    <div class="{{ {s: 4} | spacingClasses({margin: ['top']}) }}">
      {% componentJsx 'BasicPageColumn', {}, [{
        name: 'NewsletterSignup', model: newsletterModel
      }] %}
    </div>
  {% endif %}
{% endblock %}
