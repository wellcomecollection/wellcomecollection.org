apps:
  - script   : './.dist/app.js'
    name     : 'wellcomecollection.org'
    exec_mode: 'cluster'
    instances: 'max'
    # Every 6 hours
    cron_restart: '0 */6 * * *'
    env:
      NODE_ENV: "production"
