{
  "scripts": {
    "app:build": "../../node_modules/.bin/babel . --out-dir .dist --copy-files",
    "app:dev": "../../node_modules/.bin/nodemon --exec ../../node_modules/.bin/babel-node --inspect=9001 app.js"
  },
  "dependencies": {
    "common": "file:../../server",
    "immutable": "^3.8.2",
    "koa-router": "^7.3.0"
  }
}
