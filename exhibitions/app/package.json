{
  "scripts": {
    "app:dev": "nodemon -e js,njk,json --exec babel-node --inspect app.js"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "common": "file:../../server"
  },
  "devDependencies": {
    "nodemon": "^1.12.1"
  }
}
