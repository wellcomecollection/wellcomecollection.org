{
  "scripts": {
    "app:dev": "nodemon --watch . --watch node_modules --ext js,njk,json --exec babel-node --inspect=9001 app.js"
  },
  "dependencies": {
    "babel-cli": "^6.26.0",
    "common": "file:../../server"
  },
  "devDependencies": {
    "nodemon": "^1.12.1"
  }
}
